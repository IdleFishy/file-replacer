# file-replacer
Put files into a specific folder. Copy or replace.
The program will automatically create a log file in the same folder as the program. Make sure you put the program in a folder so it won't mess up your system.